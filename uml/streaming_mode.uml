@startuml
title STREAMING MODE – Real-time Gesture Recognition

actor Frontend
participant Backend
participant ESP32
participant MLModel

Frontend -> Backend: event=start_streaming
Backend -> ESP32: event=start_streaming
Backend -> Frontend: streaming_ready

loop continuous real-time stream
    ESP32 -> Backend: raw_stream
    Backend -> MLModel: predict(raw)
    MLModel --> Backend: gesture_id, gesture_name, confidence
    Backend -> Frontend: gesture_prediction
    Backend -> Backend: save streaming_prediction to DB
end

@enduml
