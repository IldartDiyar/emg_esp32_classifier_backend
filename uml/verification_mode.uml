@startuml
title VERIFICATION MODE – Prediction + Feedback

actor Frontend
participant Backend
participant ESP32
participant MLModel

Frontend -> Backend: event=start_verification (movement_id)
Backend -> ESP32: event=start_verification (movement_id)
Backend -> Frontend: verification_mode_ready

loop continuous 100ms stream
    ESP32 -> Backend: raw_stream
    Backend -> MLModel: extract_features(raw)
    MLModel --> Backend: predicted_movement, confidence
    Backend -> Frontend: verification_prediction
end

Frontend -> Backend: verification_feedback\n(movement_id, user_confirmed)
Backend -> Backend: save feedback to DB

@enduml
